import{_ as s,o as e,c as a,Q as n}from"./chunks/framework.5916b37e.js";const u=JSON.parse('{"title":"copper3d — Scene Resources","description":"","frontmatter":{},"headers":[],"relativePath":"guide/scene-resources.md","filePath":"guide/scene-resources.md"}'),o={name:"guide/scene-resources.md"},l=n(`<h1 id="copper3d-—-scene-resources" tabindex="-1">copper3d — Scene Resources <a class="header-anchor" href="#copper3d-—-scene-resources" aria-label="Permalink to &quot;copper3d — Scene Resources&quot;">​</a></h1><p>Freeing what a scene holds, bounding how much stays resident, and crossfading one model into another. All exported from the package entry (<code>copper3d</code>).</p><div class="tip custom-block"><p class="custom-block-title">New in 3.8.0</p><p>Additive only. The residency budget is <strong>off by default</strong> — nothing is evicted unless you build one and act on it.</p></div><hr><h2 id="_1-disposeobject3d-and-disposematerial" tabindex="-1">1. <code>disposeObject3D()</code> and <code>disposeMaterial()</code> <a class="header-anchor" href="#_1-disposeobject3d-and-disposematerial" aria-label="Permalink to &quot;1. \`disposeObject3D()\` and \`disposeMaterial()\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(root: DisposableObject3D): </span><span style="color:#F97583;">void</span></span>
<span class="line"><span style="color:#B392F0;">disposeMaterial</span><span style="color:#E1E4E8;">(material: DisposableMaterial </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> DisposableMaterial[] </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">null</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">undefined</span><span style="color:#E1E4E8;">): </span><span style="color:#F97583;">void</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(root: DisposableObject3D): </span><span style="color:#D73A49;">void</span></span>
<span class="line"><span style="color:#6F42C1;">disposeMaterial</span><span style="color:#24292E;">(material: DisposableMaterial </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> DisposableMaterial[] </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">null</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">undefined</span><span style="color:#24292E;">): </span><span style="color:#D73A49;">void</span></span></code></pre></div><p><code>scene.remove()</code> only unlinks. three.js keeps the geometry&#39;s buffers and the material&#39;s textures uploaded until they are disposed explicitly, and nothing in copper3d disposes what it creates — so anything that swaps models or evicts scenes needs this, or its footprint is unbounded.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.scene.</span><span style="color:#B392F0;">remove</span><span style="color:#E1E4E8;">(oldModel);</span></span>
<span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(oldModel);</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.scene.</span><span style="color:#6F42C1;">remove</span><span style="color:#24292E;">(oldModel);</span></span>
<span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(oldModel);</span></span></code></pre></div><p>Two things worth knowing:</p><ul><li><strong><code>Material.dispose()</code> does not cascade into its textures.</strong> A canvas-backed slice texture or a glTF baseColor map stays on the GPU without an explicit call. <code>disposeMaterial</code> walks the texture slots three&#39;s built-in materials use (<code>map</code>, <code>normalMap</code>, <code>roughnessMap</code>, <code>metalnessMap</code>, <code>aoMap</code>, <code>emissiveMap</code>, <code>alphaMap</code>, <code>bumpMap</code>, <code>displacementMap</code>, <code>envMap</code>, <code>lightMap</code>, <code>specularMap</code>).</li><li><strong>It handles the array form.</strong> A multi-material mesh would otherwise reach <code>Array.prototype.dispose</code>, which does not exist.</li></ul><p><code>disposeObject3D</code> works on a lone mesh as well as on a whole subtree — <code>traverse</code> visits the root itself — so a slice plane that was never added to a scene is freed by the same call. That matters for <code>loadNrrd</code>, which builds a full <code>VolumeSlice</code> (its own geometry, material and canvas-backed texture) for <strong>x, y and z</strong>, whether or not you display all three:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.</span><span style="color:#B392F0;">loadNrrd</span><span style="color:#E1E4E8;">(url, bar, </span><span style="color:#79B8FF;">true</span><span style="color:#E1E4E8;">, (</span><span style="color:#FFAB70;">volume</span><span style="color:#E1E4E8;">, </span><span style="color:#FFAB70;">meshes</span><span style="color:#E1E4E8;">, </span><span style="color:#FFAB70;">slices</span><span style="color:#E1E4E8;">) </span><span style="color:#F97583;">=&gt;</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  scene.</span><span style="color:#B392F0;">addObject</span><span style="color:#E1E4E8;">(meshes.z);</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// An axial-only viewer never shows these, and they are retained in</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// \`volume.sliceList\` for the volume&#39;s whole lifetime otherwise.</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(meshes.x);</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(meshes.y);</span></span>
<span class="line"><span style="color:#E1E4E8;">});</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.</span><span style="color:#6F42C1;">loadNrrd</span><span style="color:#24292E;">(url, bar, </span><span style="color:#005CC5;">true</span><span style="color:#24292E;">, (</span><span style="color:#E36209;">volume</span><span style="color:#24292E;">, </span><span style="color:#E36209;">meshes</span><span style="color:#24292E;">, </span><span style="color:#E36209;">slices</span><span style="color:#24292E;">) </span><span style="color:#D73A49;">=&gt;</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  scene.</span><span style="color:#6F42C1;">addObject</span><span style="color:#24292E;">(meshes.z);</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// An axial-only viewer never shows these, and they are retained in</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// \`volume.sliceList\` for the volume&#39;s whole lifetime otherwise.</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(meshes.x);</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(meshes.y);</span></span>
<span class="line"><span style="color:#24292E;">});</span></span></code></pre></div><hr><h2 id="_2-disposescene-and-removescenefrommap" tabindex="-1">2. <code>disposeScene()</code> and <code>removeSceneFromMap()</code> <a class="header-anchor" href="#_2-disposescene-and-removescenefrommap" aria-label="Permalink to &quot;2. \`disposeScene()\` and \`removeSceneFromMap()\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(host: SceneDisposalHost, name: string): DisposableScene </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">undefined</span></span>
<span class="line"><span style="color:#B392F0;">removeSceneFromMap</span><span style="color:#E1E4E8;">(host: SceneDisposalHost, name: string): </span><span style="color:#F97583;">void</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// or as a method</span></span>
<span class="line"><span style="color:#E1E4E8;">copperRendererOnDemond.</span><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(name)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(host: SceneDisposalHost, name: string): DisposableScene </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">undefined</span></span>
<span class="line"><span style="color:#6F42C1;">removeSceneFromMap</span><span style="color:#24292E;">(host: SceneDisposalHost, name: string): </span><span style="color:#D73A49;">void</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// or as a method</span></span>
<span class="line"><span style="color:#24292E;">copperRendererOnDemond.</span><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(name)</span></span></code></pre></div><p>Until 3.8.0 there was <strong>no way out of <code>sceneMap</code> at all</strong> — nothing removed an entry, so a long-lived renderer accumulated every scene it had ever built, decoded volumes included.</p><p><code>disposeScene</code> unregisters the scene, disables its controls, drops the <code>change</code> listener, and frees every child&#39;s geometry, material and textures. It returns the scene it disposed, or <code>undefined</code> if there was none under that name.</p><p><code>removeSceneFromMap</code> unregisters <strong>without freeing anything</strong>, for callers re-registering a scene under a different name.</p><h3 id="it-deliberately-does-not-call-controls-dispose" tabindex="-1">It deliberately does not call <code>controls.dispose()</code> <a class="header-anchor" href="#it-deliberately-does-not-call-controls-dispose" aria-label="Permalink to &quot;It deliberately does not call \`controls.dispose()\`&quot;">​</a></h3><p>That looks like the obvious way to break the last reference, and it is a trap. Every scene built by one renderer shares <strong>one canvas</strong>, and <code>OrbitControls.dispose()</code> calls <code>disconnect()</code>, which ends with <code>domElement.style.touchAction = &#39;&#39;</code>. Only <code>connect()</code> ever sets it back to <code>&#39;none&#39;</code>. So disposing one evicted scene&#39;s controls re-enables browser touch scrolling over the canvas for whichever scene is actually on screen — one eviction breaks touch orbiting for the rest of the session.</p><p>Removing just the <code>change</code> listener breaks the same reference chain and touches nothing shared.</p><h3 id="it-fails-loudly-if-removal-did-not-take" tabindex="-1">It fails loudly if removal did not take <a class="header-anchor" href="#it-fails-loudly-if-removal-did-not-take" aria-label="Permalink to &quot;It fails loudly if removal did not take&quot;">​</a></h3><p><code>delete</code> on a missing property is a silent no-op, so restructuring <code>sceneMap</code> into a real <code>Map</code> would stop eviction working with no crash and no type error — the map would grow without bound again, invisibly. Removal is confirmed through the host&#39;s own accessor and throws if the entry survived.</p><h3 id="it-sweeps-children-not-names" tabindex="-1">It sweeps children, not names <a class="header-anchor" href="#it-sweeps-children-not-names" aria-label="Permalink to &quot;It sweeps children, not names&quot;">​</a></h3><p>Iterating <code>scene.children</code> rather than removing objects by a hardcoded name list: a name list silently misses anything added under a name nobody thought to list.</p><hr><h2 id="_3-residency-budget" tabindex="-1">3. Residency budget <a class="header-anchor" href="#_3-residency-budget" aria-label="Permalink to &quot;3. Residency budget&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">createSceneBudget</span><span style="color:#E1E4E8;">(limitBytes: number): SceneBudget</span></span>
<span class="line"><span style="color:#B392F0;">defaultBudgetBytes</span><span style="color:#E1E4E8;">(deviceMemoryGiB</span><span style="color:#F97583;">?:</span><span style="color:#E1E4E8;"> number): number</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">createSceneBudget</span><span style="color:#24292E;">(limitBytes: number): SceneBudget</span></span>
<span class="line"><span style="color:#6F42C1;">defaultBudgetBytes</span><span style="color:#24292E;">(deviceMemoryGiB</span><span style="color:#D73A49;">?:</span><span style="color:#24292E;"> number): number</span></span></code></pre></div><p>An LRU budget <strong>in bytes</strong>, with pinning. It decides <em>which</em> scenes to evict; you call <code>disposeScene</code> on the names it hands back.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">budget</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">createSceneBudget</span><span style="color:#E1E4E8;">(</span><span style="color:#B392F0;">defaultBudgetBytes</span><span style="color:#E1E4E8;">(navigator.deviceMemory));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// after a scene finishes loading</span></span>
<span class="line"><span style="color:#E1E4E8;">budget.</span><span style="color:#B392F0;">register</span><span style="color:#E1E4E8;">(name, volume.data.byteLength);</span></span>
<span class="line"><span style="color:#E1E4E8;">budget.</span><span style="color:#B392F0;">pin</span><span style="color:#E1E4E8;">(name);                    </span><span style="color:#6A737D;">// it is on screen</span></span>
<span class="line"><span style="color:#F97583;">for</span><span style="color:#E1E4E8;"> (</span><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">victim</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">of</span><span style="color:#E1E4E8;"> budget.</span><span style="color:#B392F0;">overflow</span><span style="color:#E1E4E8;">()) {</span></span>
<span class="line"><span style="color:#E1E4E8;">  renderer.</span><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(victim);</span></span>
<span class="line"><span style="color:#E1E4E8;">  budget.</span><span style="color:#B392F0;">release</span><span style="color:#E1E4E8;">(victim);</span></span>
<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">budget</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">createSceneBudget</span><span style="color:#24292E;">(</span><span style="color:#6F42C1;">defaultBudgetBytes</span><span style="color:#24292E;">(navigator.deviceMemory));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// after a scene finishes loading</span></span>
<span class="line"><span style="color:#24292E;">budget.</span><span style="color:#6F42C1;">register</span><span style="color:#24292E;">(name, volume.data.byteLength);</span></span>
<span class="line"><span style="color:#24292E;">budget.</span><span style="color:#6F42C1;">pin</span><span style="color:#24292E;">(name);                    </span><span style="color:#6A737D;">// it is on screen</span></span>
<span class="line"><span style="color:#D73A49;">for</span><span style="color:#24292E;"> (</span><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">victim</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">of</span><span style="color:#24292E;"> budget.</span><span style="color:#6F42C1;">overflow</span><span style="color:#24292E;">()) {</span></span>
<span class="line"><span style="color:#24292E;">  renderer.</span><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(victim);</span></span>
<span class="line"><span style="color:#24292E;">  budget.</span><span style="color:#6F42C1;">release</span><span style="color:#24292E;">(victim);</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><table><thead><tr><th>method</th><th>meaning</th></tr></thead><tbody><tr><td><code>register(key, bytes)</code></td><td>Records (or re-records) a resident scene&#39;s decoded size and marks it most-recently-used.</td></tr><tr><td><code>touch(key)</code></td><td>Marks an already-registered scene most-recently-used. No-op if absent.</td></tr><tr><td><code>pin(key)</code> / <code>unpin(key)</code></td><td>Protects a scene from eviction while it is on screen.</td></tr><tr><td><code>release(key)</code></td><td>Forgets a scene entirely. Call after its GPU resources are freed.</td></tr><tr><td><code>rename(from, to)</code></td><td>Carries bytes, queue position and pin state across, for callers that swap a scene&#39;s content and re-key it.</td></tr><tr><td><code>overflow()</code></td><td>Keys to evict, least-recently-used first, never pinned. Empty when within budget or nothing evictable is left.</td></tr><tr><td><code>bytes()</code></td><td>Current total.</td></tr></tbody></table><h3 id="why-bytes-and-not-a-scene-count" tabindex="-1">Why bytes and not a scene count <a class="header-anchor" href="#why-bytes-and-not-a-scene-count" aria-label="Permalink to &quot;Why bytes and not a scene count&quot;">​</a></h3><p>A count is not the thing at risk. An NRRD volume can be 51 KB or 53 MB on disk, and NRRD decodes to a raw typed array larger still than its compressed size — so &quot;three scenes&quot; is anywhere from 150 KB to 250 MB.</p><h3 id="why-pinning" tabindex="-1">Why pinning <a class="header-anchor" href="#why-pinning" aria-label="Permalink to &quot;Why pinning&quot;">​</a></h3><p>With several viewports sharing one renderer, more than one scene is visible at a time. Whatever is on screen should be pinned and is never a victim, <strong>however far over the limit that puts the total</strong> — blanking a view the reader is looking at is strictly worse than the memory it saves. <code>overflow()</code> returns an empty array rather than evicting a pinned scene.</p><h3 id="defaultbudgetbytes" tabindex="-1"><code>defaultBudgetBytes</code> <a class="header-anchor" href="#defaultbudgetbytes" aria-label="Permalink to &quot;\`defaultBudgetBytes\`&quot;">​</a></h3><p>Returns 500 MB at or above 8 GiB of device memory, 250 MB otherwise. <code>navigator.deviceMemory</code> is a Chromium-only hint, absent in Safari and every iOS browser — and <strong>absent must mean the small budget</strong>: an iPad is both the device that omits the API and the one most likely to have its tab killed for using too much.</p><hr><h2 id="_4-model-crossfade" tabindex="-1">4. Model crossfade <a class="header-anchor" href="#_4-model-crossfade" aria-label="Permalink to &quot;4. Model crossfade&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(root: DisposableObject3D): FadeTarget[]</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(targets: FadeTarget[], factor: number): </span><span style="color:#F97583;">void</span></span>
<span class="line"><span style="color:#B392F0;">restoreFade</span><span style="color:#E1E4E8;">(targets: FadeTarget[]): </span><span style="color:#F97583;">void</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(root: DisposableObject3D): FadeTarget[]</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(targets: FadeTarget[], factor: number): </span><span style="color:#D73A49;">void</span></span>
<span class="line"><span style="color:#6F42C1;">restoreFade</span><span style="color:#24292E;">(targets: FadeTarget[]): </span><span style="color:#D73A49;">void</span></span></code></pre></div><p>Split into capture / apply / restore so the caller owns the timing — the fade is driven frame by frame from whatever animation loop you already have, rather than this starting a second one.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">outgoing</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(previous);</span></span>
<span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">incoming</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(next);</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(incoming, </span><span style="color:#79B8FF;">0</span><span style="color:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// per frame, t from 0 to 1</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(incoming, t);</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(outgoing, </span><span style="color:#79B8FF;">1</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">-</span><span style="color:#E1E4E8;"> t);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// at the end</span></span>
<span class="line"><span style="color:#B392F0;">restoreFade</span><span style="color:#E1E4E8;">(incoming);</span></span>
<span class="line"><span style="color:#E1E4E8;">scene.scene.</span><span style="color:#B392F0;">remove</span><span style="color:#E1E4E8;">(previous);</span></span>
<span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(previous);</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">outgoing</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(previous);</span></span>
<span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">incoming</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(next);</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(incoming, </span><span style="color:#005CC5;">0</span><span style="color:#24292E;">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// per frame, t from 0 to 1</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(incoming, t);</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(outgoing, </span><span style="color:#005CC5;">1</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">-</span><span style="color:#24292E;"> t);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// at the end</span></span>
<span class="line"><span style="color:#6F42C1;">restoreFade</span><span style="color:#24292E;">(incoming);</span></span>
<span class="line"><span style="color:#24292E;">scene.scene.</span><span style="color:#6F42C1;">remove</span><span style="color:#24292E;">(previous);</span></span>
<span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(previous);</span></span></code></pre></div><p>Two non-obvious rules, both encoded in <code>setFade</code>:</p><ul><li><strong>Opacity is scaled, not assigned.</strong> <code>opacity = factor</code> would end every crossfade with an already-translucent material fully opaque — a 40% translucent shell would finish at 1.0 and hide whatever it was meant to reveal.</li><li><strong><code>depthWrite</code> is suppressed for the whole fade</strong> and only restored at the fully-opaque end. A partially transparent mesh that still writes depth occludes everything drawn behind it, so a fade with depth writing left on shows the outgoing model punching holes in the incoming one. A material that never had <code>depthWrite</code> does not get it back.</li></ul><p><code>restoreFade</code> puts every captured property back exactly as <code>collectFadeTargets</code> found it. Call it at the end even if the fade was interrupted — snapping to the end state beats freezing two half-transparent models on screen.</p>`,45),t=[l];function p(c,r,i,d,y,h){return e(),a("div",null,t)}const g=s(o,[["render",p]]);export{u as __pageData,g as default};
