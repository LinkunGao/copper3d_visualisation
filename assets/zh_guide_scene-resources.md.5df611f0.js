import{_ as o,C as p,o as l,c,k as e,a as s,H as t,Q as a}from"./chunks/framework.5916b37e.js";const D=JSON.parse('{"title":"copper3d — 场景资源管理","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/scene-resources.md","filePath":"zh/guide/scene-resources.md"}'),r={name:"zh/guide/scene-resources.md"},d=a(`<h1 id="copper3d-—-场景资源管理" tabindex="-1">copper3d — 场景资源管理 <a class="header-anchor" href="#copper3d-—-场景资源管理" aria-label="Permalink to &quot;copper3d — 场景资源管理&quot;">​</a></h1><p>释放场景持有的资源、限制常驻内存总量，以及把一个模型交叉淡变成另一个。全部从包入口导出 （<code>copper3d</code>）。</p><div class="tip custom-block"><p class="custom-block-title">3.8.0 新增</p><p>纯新增。驻留预算<strong>默认关闭</strong>——不主动创建并处理它的返回值，就不会有任何东西被逐出。</p></div><hr><h2 id="_1-disposeobject3d-与-disposematerial" tabindex="-1">1. <code>disposeObject3D()</code> 与 <code>disposeMaterial()</code> <a class="header-anchor" href="#_1-disposeobject3d-与-disposematerial" aria-label="Permalink to &quot;1. \`disposeObject3D()\` 与 \`disposeMaterial()\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(root: DisposableObject3D): </span><span style="color:#F97583;">void</span></span>
<span class="line"><span style="color:#B392F0;">disposeMaterial</span><span style="color:#E1E4E8;">(material: DisposableMaterial </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> DisposableMaterial[] </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">null</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">undefined</span><span style="color:#E1E4E8;">): </span><span style="color:#F97583;">void</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(root: DisposableObject3D): </span><span style="color:#D73A49;">void</span></span>
<span class="line"><span style="color:#6F42C1;">disposeMaterial</span><span style="color:#24292E;">(material: DisposableMaterial </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> DisposableMaterial[] </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">null</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">undefined</span><span style="color:#24292E;">): </span><span style="color:#D73A49;">void</span></span></code></pre></div><p><code>scene.remove()</code> 只是解除挂接。three.js 会一直保留几何体的 buffer 和材质的贴图直到显式释放， 而 copper3d 从不释放自己创建的东西——所以任何会替换模型或逐出场景的代码都需要这两个函数， 否则内存占用没有上界。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.scene.</span><span style="color:#B392F0;">remove</span><span style="color:#E1E4E8;">(oldModel);</span></span>
<span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(oldModel);</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.scene.</span><span style="color:#6F42C1;">remove</span><span style="color:#24292E;">(oldModel);</span></span>
<span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(oldModel);</span></span></code></pre></div><p>两个值得知道的点：</p><ul><li><strong><code>Material.dispose()</code> 不会级联到它的贴图。</strong> canvas backed 的切片贴图、或 glTF 的 baseColor map，不显式调用就会一直留在 GPU 上。<code>disposeMaterial</code> 会遍历 three 内置材质使用 的贴图槽位（<code>map</code>、<code>normalMap</code>、<code>roughnessMap</code>、<code>metalnessMap</code>、<code>aoMap</code>、<code>emissiveMap</code>、 <code>alphaMap</code>、<code>bumpMap</code>、<code>displacementMap</code>、<code>envMap</code>、<code>lightMap</code>、<code>specularMap</code>）。</li><li><strong>它支持数组形式。</strong> 多材质网格否则会走到 <code>Array.prototype.dispose</code>，而那个方法并不存在。</li></ul><p><code>disposeObject3D</code> 既能处理整棵子树，也能处理单个网格——<code>traverse</code> 会访问根节点自身——所以 一个从未加入场景的切片平面用同一个调用就能释放。这对 <code>loadNrrd</code> 很要紧：它会为 <strong>x、y、z</strong> 三个方向各构建一个完整的 <code>VolumeSlice</code>（各自的几何体、材质和 canvas backed 贴图），无论你是否 显示全部三个：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.</span><span style="color:#B392F0;">loadNrrd</span><span style="color:#E1E4E8;">(url, bar, </span><span style="color:#79B8FF;">true</span><span style="color:#E1E4E8;">, (</span><span style="color:#FFAB70;">volume</span><span style="color:#E1E4E8;">, </span><span style="color:#FFAB70;">meshes</span><span style="color:#E1E4E8;">, </span><span style="color:#FFAB70;">slices</span><span style="color:#E1E4E8;">) </span><span style="color:#F97583;">=&gt;</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  scene.</span><span style="color:#B392F0;">addObject</span><span style="color:#E1E4E8;">(meshes.z);</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// 只看轴位的 viewer 永远不会显示这两个，而不释放的话它们会在</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// \`volume.sliceList\` 里跟着体数据活一辈子。</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(meshes.x);</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(meshes.y);</span></span>
<span class="line"><span style="color:#E1E4E8;">});</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.</span><span style="color:#6F42C1;">loadNrrd</span><span style="color:#24292E;">(url, bar, </span><span style="color:#005CC5;">true</span><span style="color:#24292E;">, (</span><span style="color:#E36209;">volume</span><span style="color:#24292E;">, </span><span style="color:#E36209;">meshes</span><span style="color:#24292E;">, </span><span style="color:#E36209;">slices</span><span style="color:#24292E;">) </span><span style="color:#D73A49;">=&gt;</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  scene.</span><span style="color:#6F42C1;">addObject</span><span style="color:#24292E;">(meshes.z);</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// 只看轴位的 viewer 永远不会显示这两个，而不释放的话它们会在</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// \`volume.sliceList\` 里跟着体数据活一辈子。</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(meshes.x);</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(meshes.y);</span></span>
<span class="line"><span style="color:#24292E;">});</span></span></code></pre></div><hr><h2 id="_2-disposescene-与-removescenefrommap" tabindex="-1">2. <code>disposeScene()</code> 与 <code>removeSceneFromMap()</code> <a class="header-anchor" href="#_2-disposescene-与-removescenefrommap" aria-label="Permalink to &quot;2. \`disposeScene()\` 与 \`removeSceneFromMap()\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(host: SceneDisposalHost, name: string): DisposableScene </span><span style="color:#F97583;">|</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">undefined</span></span>
<span class="line"><span style="color:#B392F0;">removeSceneFromMap</span><span style="color:#E1E4E8;">(host: SceneDisposalHost, name: string): </span><span style="color:#F97583;">void</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 或作为方法</span></span>
<span class="line"><span style="color:#E1E4E8;">copperRendererOnDemond.</span><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(name)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(host: SceneDisposalHost, name: string): DisposableScene </span><span style="color:#D73A49;">|</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">undefined</span></span>
<span class="line"><span style="color:#6F42C1;">removeSceneFromMap</span><span style="color:#24292E;">(host: SceneDisposalHost, name: string): </span><span style="color:#D73A49;">void</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 或作为方法</span></span>
<span class="line"><span style="color:#24292E;">copperRendererOnDemond.</span><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(name)</span></span></code></pre></div><p>在 3.8.0 之前，<strong>根本没有任何办法从 <code>sceneMap</code> 里移除条目</strong>——没有任何代码删除它，所以一个 长生命周期的 renderer 会累积它建过的每一个场景，包括已解码的体数据。</p><p><code>disposeScene</code> 会注销该场景、禁用它的 controls、摘掉 <code>change</code> 监听，并释放每一个子对象的几何体、 材质和贴图。返回被释放的场景，若该名字下没有场景则返回 <code>undefined</code>。</p><p><code>removeSceneFromMap</code> 只注销、<strong>不释放任何资源</strong>，供需要把场景改名重新注册的调用方使用。</p><h3 id="它故意不调用-controls-dispose" tabindex="-1">它故意不调用 <code>controls.dispose()</code> <a class="header-anchor" href="#它故意不调用-controls-dispose" aria-label="Permalink to &quot;它故意不调用 \`controls.dispose()\`&quot;">​</a></h3><p>那看上去是切断最后一处引用的显然做法，其实是个陷阱。同一个 renderer 建出的所有场景<strong>共用一块 canvas</strong>，而 <code>OrbitControls.dispose()</code> 会调 <code>disconnect()</code>，后者的最后一步是 <code>domElement.style.touchAction = &#39;&#39;</code>——而只有 <code>connect()</code> 会把它设回 <code>&#39;none&#39;</code>。于是释放一个被逐出 场景的 controls，会为<strong>当前真正显示在屏幕上</strong>的那个场景重新打开浏览器的触摸滚动：逐出一次， 整个会话剩余时间里的触摸旋转就都坏了。</p><p>只摘掉 <code>change</code> 监听同样能切断那条引用链，而且不动任何共享的东西。</p><h3 id="移除没生效时会大声报错" tabindex="-1">移除没生效时会大声报错 <a class="header-anchor" href="#移除没生效时会大声报错" aria-label="Permalink to &quot;移除没生效时会大声报错&quot;">​</a></h3><p><code>delete</code> 一个不存在的属性是静默无操作，所以如果哪天把 <code>sceneMap</code> 改成真正的 <code>Map</code>，逐出会 悄无声息地失效——不崩溃、不报类型错，而 map 会重新无界增长且完全不可见。因此移除后会通过宿主 自己的访问器确认，条目仍在就抛异常。</p><h3 id="它遍历子对象-而不是按名字清理" tabindex="-1">它遍历子对象，而不是按名字清理 <a class="header-anchor" href="#它遍历子对象-而不是按名字清理" aria-label="Permalink to &quot;它遍历子对象，而不是按名字清理&quot;">​</a></h3><p>用遍历 <code>scene.children</code> 而不是一份写死的名字列表：名字列表一定会漏掉后来有人以别的名字加进去的 东西。</p><hr>`,26),i={id:"_2a-coppersceneondemond-dispose",tabindex:"-1"},y=e("code",null,"copperSceneOnDemond.dispose()",-1),E=e("a",{class:"header-anchor",href:"#_2a-coppersceneondemond-dispose","aria-label":'Permalink to "2a. `copperSceneOnDemond.dispose()` <Badge type="tip" text="3.9.0" />"'},"​",-1),g=a(`<div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.</span><span style="color:#B392F0;">dispose</span><span style="color:#E1E4E8;">(): </span><span style="color:#F97583;">void</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.</span><span style="color:#6F42C1;">dispose</span><span style="color:#24292E;">(): </span><span style="color:#D73A49;">void</span></span></code></pre></div><p>释放场景挂在<strong>自己之外</strong>的东西 —— 具体说就是构造函数往 <code>window</code> 上挂的那个 <code>resize</code> 监听。</p><p>3.9.0 之前这个监听没有任何对应的摘除动作。每一个创建过的场景都会在整个页面生命周期里 保持订阅，被闭包吊着不放，并且在它早已不再显示之后，仍然继续调用 <code>requestRenderIfNotRequested</code> —— 进而调用 <code>onWindowResize</code>，而那会去改<strong>共享的</strong> renderer 尺寸。一个&quot;每个病例建一个场景&quot;的 app，就会每个病例泄漏一个，而且它们在每次窗口 resize 时全部触发。</p><p><code>disposeScene()</code> 现在会替你调它，所以逐出这条路已经是完整的。只有当你不经过 renderer 的 map 直接丢弃一个场景时，才需要自己调：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scene.</span><span style="color:#B392F0;">dispose</span><span style="color:#E1E4E8;">();</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scene.</span><span style="color:#6F42C1;">dispose</span><span style="color:#24292E;">();</span></span></code></pre></div><p>它不碰场景图，也不碰 renderer。场景里的东西值不值得留是调用方的决定，而 renderer 是和 其他所有场景共享的 —— 拆内容是 <code>disposeScene</code> 的活。可以重复调用；场景类没有这个方法也 没关系（<code>disposeScene</code> 是可选调用）。</p><hr><h2 id="_3-驻留预算" tabindex="-1">3. 驻留预算 <a class="header-anchor" href="#_3-驻留预算" aria-label="Permalink to &quot;3. 驻留预算&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">createSceneBudget</span><span style="color:#E1E4E8;">(limitBytes: number): SceneBudget</span></span>
<span class="line"><span style="color:#B392F0;">defaultBudgetBytes</span><span style="color:#E1E4E8;">(deviceMemoryGiB</span><span style="color:#F97583;">?:</span><span style="color:#E1E4E8;"> number): number</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">createSceneBudget</span><span style="color:#24292E;">(limitBytes: number): SceneBudget</span></span>
<span class="line"><span style="color:#6F42C1;">defaultBudgetBytes</span><span style="color:#24292E;">(deviceMemoryGiB</span><span style="color:#D73A49;">?:</span><span style="color:#24292E;"> number): number</span></span></code></pre></div><p>一个<strong>按字节</strong>计的 LRU 预算，带 pin。它只负责决定<em>该逐出哪些</em>场景；你拿它给出的名字去调 <code>disposeScene</code>。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">budget</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">createSceneBudget</span><span style="color:#E1E4E8;">(</span><span style="color:#B392F0;">defaultBudgetBytes</span><span style="color:#E1E4E8;">(navigator.deviceMemory));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 场景加载完成后</span></span>
<span class="line"><span style="color:#E1E4E8;">budget.</span><span style="color:#B392F0;">register</span><span style="color:#E1E4E8;">(name, volume.data.byteLength);</span></span>
<span class="line"><span style="color:#E1E4E8;">budget.</span><span style="color:#B392F0;">pin</span><span style="color:#E1E4E8;">(name);                    </span><span style="color:#6A737D;">// 它正显示在屏幕上</span></span>
<span class="line"><span style="color:#F97583;">for</span><span style="color:#E1E4E8;"> (</span><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">victim</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">of</span><span style="color:#E1E4E8;"> budget.</span><span style="color:#B392F0;">overflow</span><span style="color:#E1E4E8;">()) {</span></span>
<span class="line"><span style="color:#E1E4E8;">  renderer.</span><span style="color:#B392F0;">disposeScene</span><span style="color:#E1E4E8;">(victim);</span></span>
<span class="line"><span style="color:#E1E4E8;">  budget.</span><span style="color:#B392F0;">release</span><span style="color:#E1E4E8;">(victim);</span></span>
<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">budget</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">createSceneBudget</span><span style="color:#24292E;">(</span><span style="color:#6F42C1;">defaultBudgetBytes</span><span style="color:#24292E;">(navigator.deviceMemory));</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 场景加载完成后</span></span>
<span class="line"><span style="color:#24292E;">budget.</span><span style="color:#6F42C1;">register</span><span style="color:#24292E;">(name, volume.data.byteLength);</span></span>
<span class="line"><span style="color:#24292E;">budget.</span><span style="color:#6F42C1;">pin</span><span style="color:#24292E;">(name);                    </span><span style="color:#6A737D;">// 它正显示在屏幕上</span></span>
<span class="line"><span style="color:#D73A49;">for</span><span style="color:#24292E;"> (</span><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">victim</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">of</span><span style="color:#24292E;"> budget.</span><span style="color:#6F42C1;">overflow</span><span style="color:#24292E;">()) {</span></span>
<span class="line"><span style="color:#24292E;">  renderer.</span><span style="color:#6F42C1;">disposeScene</span><span style="color:#24292E;">(victim);</span></span>
<span class="line"><span style="color:#24292E;">  budget.</span><span style="color:#6F42C1;">release</span><span style="color:#24292E;">(victim);</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><table><thead><tr><th>方法</th><th>含义</th></tr></thead><tbody><tr><td><code>register(key, bytes)</code></td><td>记录（或更新）一个常驻场景的解码后大小，并标记为最近使用。</td></tr><tr><td><code>touch(key)</code></td><td>把一个已注册的场景标记为最近使用。未注册则空操作。</td></tr><tr><td><code>pin(key)</code> / <code>unpin(key)</code></td><td>场景显示在屏幕上期间保护它不被逐出。</td></tr><tr><td><code>release(key)</code></td><td>彻底忘记一个场景。在它的 GPU 资源释放之后调用。</td></tr><tr><td><code>rename(from, to)</code></td><td>把字节数、队列位置和 pin 状态一并转移，供替换场景内容并改键的调用方使用。</td></tr><tr><td><code>overflow()</code></td><td>需要逐出的 key，最久未使用的在前，永不包含被 pin 的。总量在预算内、或已无可逐出者时返回空数组。</td></tr><tr><td><code>bytes()</code></td><td>当前总量。</td></tr></tbody></table><h3 id="为什么按字节而不是按场景数量" tabindex="-1">为什么按字节而不是按场景数量 <a class="header-anchor" href="#为什么按字节而不是按场景数量" aria-label="Permalink to &quot;为什么按字节而不是按场景数量&quot;">​</a></h3><p>数量不是真正有风险的东西。一个 NRRD 体数据在磁盘上可以是 51 KB，也可以是 53 MB，而 NRRD 解码 后的原始类型化数组比压缩尺寸还要大——所以&quot;三个场景&quot;可能是 150 KB，也可能是 250 MB。</p><h3 id="为什么需要-pin" tabindex="-1">为什么需要 pin <a class="header-anchor" href="#为什么需要-pin" aria-label="Permalink to &quot;为什么需要 pin&quot;">​</a></h3><p>当多个视口共用一个 renderer 时，同时可见的场景不止一个。屏幕上的那些应当被 pin 住，并且永远 不作为逐出对象，<strong>无论这让总量超出限额多少</strong>——把读者正在看的画面清空，比它省下的那点内存 糟糕得多。<code>overflow()</code> 宁可返回空数组，也不会逐出被 pin 的场景。</p><h3 id="defaultbudgetbytes" tabindex="-1"><code>defaultBudgetBytes</code> <a class="header-anchor" href="#defaultbudgetbytes" aria-label="Permalink to &quot;\`defaultBudgetBytes\`&quot;">​</a></h3><p>设备内存 ≥ 8 GiB 返回 500 MB，否则 250 MB。<code>navigator.deviceMemory</code> 是 Chromium 独有的提示值， Safari 和所有 iOS 浏览器都没有——而**&quot;未知&quot;必须对应小预算**：iPad 既是不提供这个 API 的设备， 也是最可能因为占用过多而被系统杀掉标签页的设备。</p><hr><h2 id="_4-模型交叉淡变" tabindex="-1">4. 模型交叉淡变 <a class="header-anchor" href="#_4-模型交叉淡变" aria-label="Permalink to &quot;4. 模型交叉淡变&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(root: DisposableObject3D): FadeTarget[]</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(targets: FadeTarget[], factor: number): </span><span style="color:#F97583;">void</span></span>
<span class="line"><span style="color:#B392F0;">restoreFade</span><span style="color:#E1E4E8;">(targets: FadeTarget[]): </span><span style="color:#F97583;">void</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(root: DisposableObject3D): FadeTarget[]</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(targets: FadeTarget[], factor: number): </span><span style="color:#D73A49;">void</span></span>
<span class="line"><span style="color:#6F42C1;">restoreFade</span><span style="color:#24292E;">(targets: FadeTarget[]): </span><span style="color:#D73A49;">void</span></span></code></pre></div><p>拆成&quot;采集 / 施加 / 还原&quot;三步，时序由调用方掌握——淡变由你已有的动画循环逐帧驱动，而不是由这里 再起一个循环。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">outgoing</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(previous);</span></span>
<span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">incoming</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">collectFadeTargets</span><span style="color:#E1E4E8;">(next);</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(incoming, </span><span style="color:#79B8FF;">0</span><span style="color:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 每帧，t 从 0 到 1</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(incoming, t);</span></span>
<span class="line"><span style="color:#B392F0;">setFade</span><span style="color:#E1E4E8;">(outgoing, </span><span style="color:#79B8FF;">1</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">-</span><span style="color:#E1E4E8;"> t);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 结束时</span></span>
<span class="line"><span style="color:#B392F0;">restoreFade</span><span style="color:#E1E4E8;">(incoming);</span></span>
<span class="line"><span style="color:#E1E4E8;">scene.scene.</span><span style="color:#B392F0;">remove</span><span style="color:#E1E4E8;">(previous);</span></span>
<span class="line"><span style="color:#B392F0;">disposeObject3D</span><span style="color:#E1E4E8;">(previous);</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">outgoing</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(previous);</span></span>
<span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">incoming</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">collectFadeTargets</span><span style="color:#24292E;">(next);</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(incoming, </span><span style="color:#005CC5;">0</span><span style="color:#24292E;">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 每帧，t 从 0 到 1</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(incoming, t);</span></span>
<span class="line"><span style="color:#6F42C1;">setFade</span><span style="color:#24292E;">(outgoing, </span><span style="color:#005CC5;">1</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">-</span><span style="color:#24292E;"> t);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// 结束时</span></span>
<span class="line"><span style="color:#6F42C1;">restoreFade</span><span style="color:#24292E;">(incoming);</span></span>
<span class="line"><span style="color:#24292E;">scene.scene.</span><span style="color:#6F42C1;">remove</span><span style="color:#24292E;">(previous);</span></span>
<span class="line"><span style="color:#6F42C1;">disposeObject3D</span><span style="color:#24292E;">(previous);</span></span></code></pre></div><p>两条不显然的规则，都编码在 <code>setFade</code> 里：</p><ul><li><strong>opacity 是按比例缩放，不是直接赋值。</strong> <code>opacity = factor</code> 会让本来就半透明的材质在淡变结束 时变成完全不透明——一层 40% 透明的外壳会停在 1.0，把它本该透出来的东西全遮住。</li><li><strong><code>depthWrite</code> 在整个淡变过程中被压掉</strong>，只在完全不透明的那一端才恢复。半透明面片如果还写 深度，就会遮挡画在它后面的一切；淡变时若不关掉，会看到淡出的模型在淡入的模型上&quot;打洞&quot;。 本来就没有 <code>depthWrite</code> 的材质不会被恢复出来。</li></ul><p><code>restoreFade</code> 会把每一个采集到的属性精确还原成 <code>collectFadeTargets</code> 当初看到的样子。<strong>即使淡变 被中断也要在结束时调用它</strong>——直接跳到终态，好过让两个半透明模型冻结在屏幕上。</p>`,26);function h(u,F,m,b,v,C){const n=p("Badge");return l(),c("div",null,[d,e("h2",i,[s("2a. "),y,s(),t(n,{type:"tip",text:"3.9.0"}),s(),E]),g])}const k=o(r,[["render",h]]);export{D as __pageData,k as default};
