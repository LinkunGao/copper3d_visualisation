import{_ as s,o as e,c as t,Q as a}from"./chunks/framework.5916b37e.js";const g=JSON.parse('{"title":"Rotating Around the Content, Not the Pan","description":"","frontmatter":{},"headers":[],"relativePath":"guide/rotation-pivot.md","filePath":"guide/rotation-pivot.md"}'),o={name:"guide/rotation-pivot.md"},n=a(`<h1 id="rotating-around-the-content-not-the-pan" tabindex="-1">Rotating Around the Content, Not the Pan <a class="header-anchor" href="#rotating-around-the-content-not-the-pan" aria-label="Permalink to &quot;Rotating Around the Content, Not the Pan&quot;">​</a></h1><p><code>Copper3dTrackballControls.rotationPivot</code> decides what a rotate gesture does after the reader has panned. It is the answer to &quot;why does my model swing off into space once I&#39;ve moved it?&quot;</p><div class="tip custom-block"><p class="custom-block-title">New in 3.10.0</p><p>Additive and opt-in. <code>rotationPivot</code> defaults to <code>null</code>, which is exactly the behaviour every earlier version had — leave it alone and nothing changes.</p></div><p>For a drop-in helper that switches between all three behaviours at runtime, and how to tell them apart on screen, see <a href="/copper3d_visualisation/examples/rotation-pivot-switch.html">Switching Rotation Behaviour After a Pan</a>.</p><hr><h2 id="_1-the-problem" tabindex="-1">1. The problem <a class="header-anchor" href="#_1-the-problem" aria-label="Permalink to &quot;1. The problem&quot;">​</a></h2><p><code>panCamera()</code> translates the <strong>camera and <code>target</code> together</strong>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">scope.object.position.</span><span style="color:#B392F0;">add</span><span style="color:#E1E4E8;">(pan);</span></span>
<span class="line"><span style="color:#E1E4E8;">scope.target.</span><span style="color:#B392F0;">add</span><span style="color:#E1E4E8;">(pan);          </span><span style="color:#6A737D;">// &lt;- the pivot moves with the camera</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">scope.object.position.</span><span style="color:#6F42C1;">add</span><span style="color:#24292E;">(pan);</span></span>
<span class="line"><span style="color:#24292E;">scope.target.</span><span style="color:#6F42C1;">add</span><span style="color:#24292E;">(pan);          </span><span style="color:#6A737D;">// &lt;- the pivot moves with the camera</span></span></code></pre></div><p>That is what makes a pan a pan: the view slides sideways and the framing stays put. But <code>target</code> is also the point rotation orbits, because <code>rotateCamera()</code> works on <code>_eye = position - target</code> and <code>update()</code> puts the camera back at <code>target + eye</code>.</p><p>So the moment someone pans, the orbit pivot is no longer on the content. The next rotate swings the volume through a wide arc around an empty point in space instead of turning it on the spot. The further they panned, the worse it gets — and nothing throws, nothing warns, it just feels broken.</p><h2 id="_2-the-obvious-fix-and-what-it-costs" tabindex="-1">2. The obvious fix, and what it costs <a class="header-anchor" href="#_2-the-obvious-fix-and-what-it-costs" aria-label="Permalink to &quot;2. The obvious fix, and what it costs&quot;">​</a></h2><p>The usual repair is to snap <code>target</code> back onto the content&#39;s centre when a rotate starts. <code>Copper3dOrbitControls</code> does exactly this, on the controls&#39; <code>start</code> event:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#79B8FF;">this</span><span style="color:#E1E4E8;">.</span><span style="color:#B392F0;">_onStart</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> () </span><span style="color:#F97583;">=&gt;</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#F97583;">if</span><span style="color:#E1E4E8;"> (</span><span style="color:#B392F0;">isRotateGesture</span><span style="color:#E1E4E8;">(</span><span style="color:#79B8FF;">this</span><span style="color:#E1E4E8;">.state)) </span><span style="color:#79B8FF;">this</span><span style="color:#E1E4E8;">.target.</span><span style="color:#B392F0;">copy</span><span style="color:#E1E4E8;">(</span><span style="color:#79B8FF;">this</span><span style="color:#E1E4E8;">._pivot);</span></span>
<span class="line"><span style="color:#E1E4E8;">};</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#005CC5;">this</span><span style="color:#24292E;">.</span><span style="color:#6F42C1;">_onStart</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> () </span><span style="color:#D73A49;">=&gt;</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#D73A49;">if</span><span style="color:#24292E;"> (</span><span style="color:#6F42C1;">isRotateGesture</span><span style="color:#24292E;">(</span><span style="color:#005CC5;">this</span><span style="color:#24292E;">.state)) </span><span style="color:#005CC5;">this</span><span style="color:#24292E;">.target.</span><span style="color:#6F42C1;">copy</span><span style="color:#24292E;">(</span><span style="color:#005CC5;">this</span><span style="color:#24292E;">._pivot);</span></span>
<span class="line"><span style="color:#24292E;">};</span></span></code></pre></div><p>It fixes the axis. It also <strong>throws the pan away</strong>: assigning <code>target</code> does not move the camera — <code>update()</code> re-derives the offset from the camera&#39;s current position — but <code>object.lookAt(target)</code> re-aims it, so the content slides back to the middle of the viewport as the rotation begins.</p><p>For many viewers that is fine, even desirable. For a reader who panned deliberately — to put a lesion against the edge of a panel, next to a measurement — it is not.</p><h2 id="_3-rotationpivot" tabindex="-1">3. <code>rotationPivot</code> <a class="header-anchor" href="#_3-rotationpivot" aria-label="Permalink to &quot;3. \`rotationPivot\`&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#E1E4E8;">controls.rotationPivot </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">new</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">THREE</span><span style="color:#E1E4E8;">.</span><span style="color:#B392F0;">Vector3</span><span style="color:#E1E4E8;">(cx, cy, cz);   </span><span style="color:#6A737D;">// world space</span></span>
<span class="line"><span style="color:#E1E4E8;">controls.rotationPivot </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">null</span><span style="color:#E1E4E8;">;                            </span><span style="color:#6A737D;">// back to orbiting target</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">controls.rotationPivot </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">new</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">THREE</span><span style="color:#24292E;">.</span><span style="color:#6F42C1;">Vector3</span><span style="color:#24292E;">(cx, cy, cz);   </span><span style="color:#6A737D;">// world space</span></span>
<span class="line"><span style="color:#24292E;">controls.rotationPivot </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">null</span><span style="color:#24292E;">;                            </span><span style="color:#6A737D;">// back to orbiting target</span></span></code></pre></div><table><thead><tr><th></th><th>type</th><th>default</th></tr></thead><tbody><tr><td><code>rotationPivot</code></td><td><code>Vector3 | null</code></td><td><code>null</code></td></tr></tbody></table><p><strong>Nothing here is on by default.</strong> The default is <code>null</code>, which is the stock trackball behaviour described in section 1 — a pan drags the orbit pivot with it. That is true in 3.10.0 as much as in every earlier version; you have to set the pivot for anything to change.</p><p>With a pivot set, the frame&#39;s rotation <code>q</code> is applied to <code>target - pivot</code> as well as to <code>_eye</code> and <code>object.up</code>. Writing <code>C</code> for the pivot:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#e1e4e8;">target&#39;   = C + q(target − C)</span></span>
<span class="line"><span style="color:#e1e4e8;">eye&#39;      = q(eye)</span></span>
<span class="line"><span style="color:#e1e4e8;">position&#39; = target&#39; + eye&#39;</span></span>
<span class="line"><span style="color:#e1e4e8;">          = C + q(target − C) + q(position − target)</span></span>
<span class="line"><span style="color:#e1e4e8;">          = C + q(position − C)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">target&#39;   = C + q(target − C)</span></span>
<span class="line"><span style="color:#24292e;">eye&#39;      = q(eye)</span></span>
<span class="line"><span style="color:#24292e;">position&#39; = target&#39; + eye&#39;</span></span>
<span class="line"><span style="color:#24292e;">          = C + q(target − C) + q(position − target)</span></span>
<span class="line"><span style="color:#24292e;">          = C + q(position − C)</span></span></code></pre></div><p>The whole camera rig — camera <em>and</em> target — rotates rigidly about <code>C</code>. Two things follow:</p><ul><li><strong>Every distance to <code>C</code> is preserved.</strong> The content turns about its own centre.</li><li><strong><code>C</code>&#39;s projection on screen does not move.</strong> The pan offset survives: the content stays exactly where it was dragged to while it rotates.</li></ul><p>When nothing has been panned, <code>target</code> <em>is</em> the pivot, so the offset being rotated is the zero vector and the whole thing is a no-op. A viewer that never pans cannot tell the difference.</p><h2 id="_4-using-it" tabindex="-1">4. Using it <a class="header-anchor" href="#_4-using-it" aria-label="Permalink to &quot;4. Using it&quot;">​</a></h2><p>Set the pivot once per piece of content, in world space:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">box</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">new</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">THREE</span><span style="color:#E1E4E8;">.</span><span style="color:#B392F0;">Box3</span><span style="color:#E1E4E8;">();</span></span>
<span class="line"><span style="color:#E1E4E8;">box.</span><span style="color:#B392F0;">expandByObject</span><span style="color:#E1E4E8;">(nrrdMesh.x);</span></span>
<span class="line"><span style="color:#E1E4E8;">box.</span><span style="color:#B392F0;">expandByObject</span><span style="color:#E1E4E8;">(nrrdMesh.y);</span></span>
<span class="line"><span style="color:#E1E4E8;">box.</span><span style="color:#B392F0;">expandByObject</span><span style="color:#E1E4E8;">(nrrdMesh.z);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8;">controls.rotationPivot </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> box.</span><span style="color:#B392F0;">getCenter</span><span style="color:#E1E4E8;">(</span><span style="color:#F97583;">new</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">THREE</span><span style="color:#E1E4E8;">.</span><span style="color:#B392F0;">Vector3</span><span style="color:#E1E4E8;">());</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">box</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">new</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">THREE</span><span style="color:#24292E;">.</span><span style="color:#6F42C1;">Box3</span><span style="color:#24292E;">();</span></span>
<span class="line"><span style="color:#24292E;">box.</span><span style="color:#6F42C1;">expandByObject</span><span style="color:#24292E;">(nrrdMesh.x);</span></span>
<span class="line"><span style="color:#24292E;">box.</span><span style="color:#6F42C1;">expandByObject</span><span style="color:#24292E;">(nrrdMesh.y);</span></span>
<span class="line"><span style="color:#24292E;">box.</span><span style="color:#6F42C1;">expandByObject</span><span style="color:#24292E;">(nrrdMesh.z);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#24292E;">controls.rotationPivot </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> box.</span><span style="color:#6F42C1;">getCenter</span><span style="color:#24292E;">(</span><span style="color:#D73A49;">new</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">THREE</span><span style="color:#24292E;">.</span><span style="color:#6F42C1;">Vector3</span><span style="color:#24292E;">());</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">It is world space, and it does not follow anything</p><p>Measure it from the objects themselves rather than assuming the origin. Slice planes carry the volume&#39;s own offset and a GLB&#39;s bounding box is rarely centred, so the world origin is usually the wrong point — the same mistake <code>fitView</code> avoids with <code>bounds.center</code>.</p><p>Recompute it whenever the content changes (a new case, a new volume). Nothing invalidates it for you.</p></div><p>Clearing it is a normal thing to do, not a teardown step — a scene where the reader should be able to orbit whatever they panned to still wants <code>null</code>.</p><h3 id="with-fitview-setcamerapose" tabindex="-1">With <code>fitView</code> / <code>setCameraPose</code> <a class="header-anchor" href="#with-fitview-setcamerapose" aria-label="Permalink to &quot;With \`fitView\` / \`setCameraPose\`&quot;">​</a></h3><p>No interaction to manage. Both write <code>controls.target</code>, and both aim at the content&#39;s centre, so after a re-frame or a &quot;reset view&quot; the target and the pivot coincide again and the pan offset is genuinely zero. Set the pivot from the same box you pass to <code>fitView</code> as <code>bounds.center</code> and the two agree by construction.</p><h3 id="feature-detection" tabindex="-1">Feature detection <a class="header-anchor" href="#feature-detection" aria-label="Permalink to &quot;Feature detection&quot;">​</a></h3><p>The property is a plain field initialised in the constructor, so <code>in</code> is enough to tell an older build apart and fall back to the snap-on-rotate pattern:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">if</span><span style="color:#E1E4E8;"> (</span><span style="color:#9ECBFF;">&quot;rotationPivot&quot;</span><span style="color:#E1E4E8;"> </span><span style="color:#F97583;">in</span><span style="color:#E1E4E8;"> controls) {</span></span>
<span class="line"><span style="color:#E1E4E8;">  controls.rotationPivot </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> centre;</span></span>
<span class="line"><span style="color:#E1E4E8;">} </span><span style="color:#F97583;">else</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// 3.9.x and earlier: snap the target on rotate start instead, accepting that</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// the pan is discarded. Register in the CAPTURE phase — the trackball latches</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">// its gesture state in its own bubble-phase pointerdown handler on the canvas.</span></span>
<span class="line"><span style="color:#E1E4E8;">  container.</span><span style="color:#B392F0;">addEventListener</span><span style="color:#E1E4E8;">(</span><span style="color:#9ECBFF;">&quot;pointerdown&quot;</span><span style="color:#E1E4E8;">, (</span><span style="color:#FFAB70;">event</span><span style="color:#E1E4E8;">) </span><span style="color:#F97583;">=&gt;</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">    </span><span style="color:#F97583;">if</span><span style="color:#E1E4E8;"> (</span><span style="color:#B392F0;">isRotateButton</span><span style="color:#E1E4E8;">(event)) controls.target.</span><span style="color:#B392F0;">copy</span><span style="color:#E1E4E8;">(centre);</span></span>
<span class="line"><span style="color:#E1E4E8;">  }, </span><span style="color:#79B8FF;">true</span><span style="color:#E1E4E8;">);</span></span>
<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">if</span><span style="color:#24292E;"> (</span><span style="color:#032F62;">&quot;rotationPivot&quot;</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">in</span><span style="color:#24292E;"> controls) {</span></span>
<span class="line"><span style="color:#24292E;">  controls.rotationPivot </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> centre;</span></span>
<span class="line"><span style="color:#24292E;">} </span><span style="color:#D73A49;">else</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// 3.9.x and earlier: snap the target on rotate start instead, accepting that</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// the pan is discarded. Register in the CAPTURE phase — the trackball latches</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">// its gesture state in its own bubble-phase pointerdown handler on the canvas.</span></span>
<span class="line"><span style="color:#24292E;">  container.</span><span style="color:#6F42C1;">addEventListener</span><span style="color:#24292E;">(</span><span style="color:#032F62;">&quot;pointerdown&quot;</span><span style="color:#24292E;">, (</span><span style="color:#E36209;">event</span><span style="color:#24292E;">) </span><span style="color:#D73A49;">=&gt;</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#D73A49;">if</span><span style="color:#24292E;"> (</span><span style="color:#6F42C1;">isRotateButton</span><span style="color:#24292E;">(event)) controls.target.</span><span style="color:#6F42C1;">copy</span><span style="color:#24292E;">(centre);</span></span>
<span class="line"><span style="color:#24292E;">  }, </span><span style="color:#005CC5;">true</span><span style="color:#24292E;">);</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><h2 id="_5-what-it-does-not-change" tabindex="-1">5. What it does not change <a class="header-anchor" href="#_5-what-it-does-not-change" aria-label="Permalink to &quot;5. What it does not change&quot;">​</a></h2><ul><li><strong>Zoom still moves toward <code>target</code>,</strong> not toward the pivot — <code>zoomCamera()</code> scales <code>_eye</code>. That is what keeps the framing steady while zooming a panned view; zooming toward the pivot would drag the content back toward the centre.</li><li><strong><code>minDistance</code> / <code>maxDistance</code> still clamp <code>|position − target|</code>,</strong> not the distance to the pivot. With a large pan those differ slightly; the limits are soft-feel bounds, so it does not matter in practice.</li><li><strong>Damping is handled.</strong> The glide after the pointer is released rotates the target too, so the rig cannot come apart over those frames.</li><li><strong><code>noRotate</code>, <code>enabled</code> and <code>updateOnInput</code> are untouched.</strong> A locked axis stays locked; this only changes <em>what</em> a permitted rotation turns about.</li></ul><h2 id="_6-which-one-should-you-use" tabindex="-1">6. Which one should you use? <a class="header-anchor" href="#_6-which-one-should-you-use" aria-label="Permalink to &quot;6. Which one should you use?&quot;">​</a></h2><p>There are <strong>three</strong> states, not two — doing nothing is one of them, and it is where you start:</p><table><thead><tr><th></th><th>how you get it</th><th>rotation axis</th><th>a pan the reader made</th><th>needs</th></tr></thead><tbody><tr><td><strong>orbit-target</strong></td><td>do nothing — the default</td><td>the panned <code>target</code></td><td>kept, but the axis is wrong</td><td>—</td></tr><tr><td><strong>recentre</strong></td><td>snap <code>target</code> on rotate start</td><td>content&#39;s centre</td><td>discarded when rotation starts</td><td>any version</td></tr><tr><td><strong>keep-pan</strong></td><td><code>rotationPivot = centre</code></td><td>content&#39;s centre</td><td>kept</td><td>3.10.0+</td></tr></tbody></table><p>The first row is the behaviour this page exists to fix, so the real choice is between the last two. Both make &quot;the centre of rotation is the centre of the image&quot; true; they differ only in what happens to the pan, so pick on that.</p><p><strong>Never run the last two at once</strong> — the snap would discard the very offset the pivot is preserving, and you would get <code>recentre</code> while believing you configured <code>keep-pan</code>.</p><p>To see which one a viewer is currently in: pan the content into a corner of the viewport, then drag to rotate. It swings away on a wide arc (<code>orbit-target</code>), snaps back to the middle and then turns (<code>recentre</code>), or stays in the corner and turns (<code>keep-pan</code>).</p><h2 id="_7-upgrading-from-3-9-x" tabindex="-1">7. Upgrading from 3.9.x <a class="header-anchor" href="#_7-upgrading-from-3-9-x" aria-label="Permalink to &quot;7. Upgrading from 3.9.x&quot;">​</a></h2><p>Nothing to do. The default is <code>null</code> and the rotation path is byte-identical to before when it is unset or equal to <code>target</code>.</p><p>If you are currently snapping <code>target</code> on rotate start, <strong>remove that</strong> when you set a pivot — the snap would throw away the very offset the pivot is preserving.</p><h2 id="_8-tests" tabindex="-1">8. Tests <a class="header-anchor" href="#_8-tests" aria-label="Permalink to &quot;8. Tests&quot;">​</a></h2><p><code>src/ts/__tests__/trackballRotationPivot.test.ts</code> pins the rigid-rotation invariants: distance to the pivot, the pan offset&#39;s length, and the camera-to-target distance all survive a rotate, the camera actually moves, and an un-panned viewer produces identical output with and without a pivot. These are all silent failures — each wrong version still renders a plausible picture — so they are worth asserting rather than eyeballing.</p>`,47),l=[n];function p(r,c,i,d,h,y){return e(),t("div",null,l)}const u=s(o,[["render",p]]);export{g as __pageData,u as default};
