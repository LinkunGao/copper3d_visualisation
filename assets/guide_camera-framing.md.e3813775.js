import{_ as s,o as e,c as a,Q as n}from"./chunks/framework.5916b37e.js";const u=JSON.parse('{"title":"copper3d — Camera Framing & Transitions","description":"","frontmatter":{},"headers":[],"relativePath":"guide/camera-framing.md","filePath":"guide/camera-framing.md"}'),o={name:"guide/camera-framing.md"},t=n(`<h1 id="copper3d-—-camera-framing-transitions" tabindex="-1">copper3d — Camera Framing &amp; Transitions <a class="header-anchor" href="#copper3d-—-camera-framing-transitions" aria-label="Permalink to &quot;copper3d — Camera Framing &amp; Transitions&quot;">​</a></h1><p>Framing a camera on its content, and moving it from one pose to another. All exported from the package entry (<code>copper3d</code>).</p><div class="tip custom-block"><p class="custom-block-title">New in 3.8.0</p><p>Additive only. <code>computeFraming</code>, <code>resolveViewPose</code>, <code>resolveFarPlane</code>, <code>Copper3dOrbitControls</code>, <code>loadView</code> and <code>loadGltf</code>&#39;s own camera handling are unchanged — call none of the below and the camera behaves exactly as it did in 3.7.x.</p></div><hr><h2 id="_1-fitdistance" tabindex="-1">1. <code>fitDistance()</code> <a class="header-anchor" href="#_1-fitdistance" aria-label="Permalink to &quot;1. \`fitDistance()\`&quot;">​</a></h2><p>How far a perspective camera must sit from an object&#39;s centre for the whole object to fit in frame.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#B392F0;">fitDistance</span><span style="color:#E1E4E8;">(bounds: FitBounds, aspect: number, fovDeg: number, margin</span><span style="color:#F97583;">?:</span><span style="color:#E1E4E8;"> number): number</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">fitDistance</span><span style="color:#24292E;">(bounds: FitBounds, aspect: number, fovDeg: number, margin</span><span style="color:#D73A49;">?:</span><span style="color:#24292E;"> number): number</span></span></code></pre></div><table><thead><tr><th>parameter</th><th>type</th><th>default</th><th>meaning</th></tr></thead><tbody><tr><td><code>bounds</code></td><td><code>FitBounds</code></td><td>—</td><td>The object&#39;s axis-aligned size and centre, in scene units.</td></tr><tr><td><code>aspect</code></td><td><code>number</code></td><td>—</td><td>Viewport width / height. Zero or non-finite is treated as <code>1</code>.</td></tr><tr><td><code>fovDeg</code></td><td><code>number</code></td><td>—</td><td>The camera&#39;s <strong>vertical</strong> field of view, in degrees.</td></tr><tr><td><code>margin</code></td><td><code>number</code></td><td><code>0.85</code></td><td>Multiplier applied at the end. Smaller means larger on screen.</td></tr></tbody></table><h3 id="how-it-differs-from-computeframing" tabindex="-1">How it differs from <code>computeFraming</code> <a class="header-anchor" href="#how-it-differs-from-computeframing" aria-label="Permalink to &quot;How it differs from \`computeFraming\`&quot;">​</a></h3><p><code>computeFraming</code> (unchanged, still used by <code>Copper3dOrbitControls</code>) uses the bounding-box diagonal and the vertical FOV alone. <code>fitDistance</code> differs in two ways.</p><p><strong>It takes aspect into account.</strong> Below aspect 1 the <em>horizontal</em> field is the narrower of the two, and it is the narrower one that has to contain the object. A camera framed on vertical FOV alone under-frames every tall, narrow viewport — which is what happens the moment a viewer is split into side-by-side panels.</p><p><strong>It fits the bounding sphere, not the box.</strong> Fitting the facing box face plus half the depth frames each object by whichever dimension happens to point at the camera. A thin slab (a mammogram volume, 33 slices) barely moves the camera and fills its panel edge to edge, while a near-cubic MRI volume of comparable overall extent is pushed back far enough to look half the size beside it. A sphere has no orientation, so comparable objects get comparable screen size — and orbiting can no longer push a corner out of frame.</p><h3 id="about-margin" tabindex="-1">About <code>margin</code> <a class="header-anchor" href="#about-margin" aria-label="Permalink to &quot;About \`margin\`&quot;">​</a></h3><p>It is deliberately <strong>below 1</strong>. A bounding sphere circumscribes the object — a cube&#39;s has 1.73× its half-side — so fitting the sphere exactly leaves the object filling well under half the frame. Letting the sphere overflow a little is what makes the object itself read at a sensible size.</p><p>This is a tuning knob, not a correctness threshold. <code>1.35</code> and <code>1.05</code> both read as too small against real panels; <code>0.75</code> was too large, with the volume&#39;s bounding cage pressed against the panel edges.</p><hr><h2 id="_2-fitview" tabindex="-1">2. <code>fitView()</code> <a class="header-anchor" href="#_2-fitview" aria-label="Permalink to &quot;2. \`fitView()\`&quot;">​</a></h2><p>Re-frames a scene on its content, keeping the view preset&#39;s direction.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#6A737D;">// free function</span></span>
<span class="line"><span style="color:#B392F0;">fitView</span><span style="color:#E1E4E8;">(</span></span>
<span class="line"><span style="color:#E1E4E8;">  scene: FitViewScene,</span></span>
<span class="line"><span style="color:#E1E4E8;">  preset: CameraViewPreset,</span></span>
<span class="line"><span style="color:#E1E4E8;">  aspect: number,</span></span>
<span class="line"><span style="color:#E1E4E8;">  bounds: FitBounds,</span></span>
<span class="line"><span style="color:#E1E4E8;">  margin</span><span style="color:#F97583;">?:</span><span style="color:#E1E4E8;"> number</span></span>
<span class="line"><span style="color:#E1E4E8;">): boolean</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// or as a method</span></span>
<span class="line"><span style="color:#E1E4E8;">copperSceneOnDemond.</span><span style="color:#B392F0;">fitView</span><span style="color:#E1E4E8;">(preset, aspect, bounds, margin</span><span style="color:#F97583;">?</span><span style="color:#E1E4E8;">)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6A737D;">// free function</span></span>
<span class="line"><span style="color:#6F42C1;">fitView</span><span style="color:#24292E;">(</span></span>
<span class="line"><span style="color:#24292E;">  scene: FitViewScene,</span></span>
<span class="line"><span style="color:#24292E;">  preset: CameraViewPreset,</span></span>
<span class="line"><span style="color:#24292E;">  aspect: number,</span></span>
<span class="line"><span style="color:#24292E;">  bounds: FitBounds,</span></span>
<span class="line"><span style="color:#24292E;">  margin</span><span style="color:#D73A49;">?:</span><span style="color:#24292E;"> number</span></span>
<span class="line"><span style="color:#24292E;">): boolean</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D;">// or as a method</span></span>
<span class="line"><span style="color:#24292E;">copperSceneOnDemond.</span><span style="color:#6F42C1;">fitView</span><span style="color:#24292E;">(preset, aspect, bounds, margin</span><span style="color:#D73A49;">?</span><span style="color:#24292E;">)</span></span></code></pre></div><p>Returns <code>false</code> when there is nothing to do: an orthographic camera (no field of view to fit against), or a preset whose eye sits exactly on its target. <strong>Does not render</strong> — the caller decides when to draw, which matters under on-demand rendering.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> { </span><span style="color:#79B8FF;">width</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">height</span><span style="color:#E1E4E8;"> } </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> container.</span><span style="color:#B392F0;">getBoundingClientRect</span><span style="color:#E1E4E8;">();</span></span>
<span class="line"><span style="color:#F97583;">const</span><span style="color:#E1E4E8;"> [</span><span style="color:#79B8FF;">x</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">y</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">z</span><span style="color:#E1E4E8;">] </span><span style="color:#F97583;">=</span><span style="color:#E1E4E8;"> volume.RASDimensions;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8;">scene.</span><span style="color:#B392F0;">fitView</span><span style="color:#E1E4E8;">(preset, width </span><span style="color:#F97583;">/</span><span style="color:#E1E4E8;"> height, {</span></span>
<span class="line"><span style="color:#E1E4E8;">  width: x, height: y, depth: z,</span></span>
<span class="line"><span style="color:#E1E4E8;">  center: [</span><span style="color:#79B8FF;">0</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">0</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">0</span><span style="color:#E1E4E8;">],</span></span>
<span class="line"><span style="color:#E1E4E8;">});</span></span>
<span class="line"><span style="color:#E1E4E8;">renderer.</span><span style="color:#B392F0;">render</span><span style="color:#E1E4E8;">();</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> { </span><span style="color:#005CC5;">width</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">height</span><span style="color:#24292E;"> } </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> container.</span><span style="color:#6F42C1;">getBoundingClientRect</span><span style="color:#24292E;">();</span></span>
<span class="line"><span style="color:#D73A49;">const</span><span style="color:#24292E;"> [</span><span style="color:#005CC5;">x</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">y</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">z</span><span style="color:#24292E;">] </span><span style="color:#D73A49;">=</span><span style="color:#24292E;"> volume.RASDimensions;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#24292E;">scene.</span><span style="color:#6F42C1;">fitView</span><span style="color:#24292E;">(preset, width </span><span style="color:#D73A49;">/</span><span style="color:#24292E;"> height, {</span></span>
<span class="line"><span style="color:#24292E;">  width: x, height: y, depth: z,</span></span>
<span class="line"><span style="color:#24292E;">  center: [</span><span style="color:#005CC5;">0</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">0</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">0</span><span style="color:#24292E;">],</span></span>
<span class="line"><span style="color:#24292E;">});</span></span>
<span class="line"><span style="color:#24292E;">renderer.</span><span style="color:#6F42C1;">render</span><span style="color:#24292E;">();</span></span></code></pre></div><h3 id="what-it-keeps-and-what-it-replaces" tabindex="-1">What it keeps and what it replaces <a class="header-anchor" href="#what-it-keeps-and-what-it-replaces" aria-label="Permalink to &quot;What it keeps and what it replaces&quot;">​</a></h3><p>A view preset carries a hand-written <code>eyePosition</code>, which encodes two different things: which way a reader should look at the data, and how far away. Only the first is a decision. The second cannot survive a viewport a third as wide as the one it was authored against, and hand-written values routinely leave the content filling a quarter of the frame.</p><p>So the preset&#39;s <strong>view direction and up vector are kept exactly as authored</strong> — they are often a clinical decision — and only the distance is recomputed from <code>fitDistance</code>.</p><h3 id="it-aims-at-the-object-s-centre-not-the-preset-s-target" tabindex="-1">It aims at the object&#39;s centre, not the preset&#39;s target <a class="header-anchor" href="#it-aims-at-the-object-s-centre-not-the-preset-s-target" aria-label="Permalink to &quot;It aims at the object&#39;s centre, not the preset&#39;s target&quot;">​</a></h3><p>Presets normally target the origin. That is right for an NRRD volume, whose <code>RASDimensions</code> describe a box centred there, and wrong for a GLB whose bounding box is not: framing one of those from the origin pushes part of it out of frame, which a narrow viewport makes obvious. Pass the real centre in <code>bounds.center</code> — <code>[0, 0, 0]</code> for a volume makes this a no-op.</p><h3 id="it-syncs-controls-target" tabindex="-1">It syncs <code>controls.target</code> <a class="header-anchor" href="#it-syncs-controls-target" aria-label="Permalink to &quot;It syncs \`controls.target\`&quot;">​</a></h3><p><strong>This is the load-bearing half.</strong> Nothing syncs the controls&#39; orbit pivot with <code>camera.lookAt()</code>, so without it the user&#39;s next drag calls <code>controls.update()</code>, which re-aims the camera at whatever <code>target</code> still held — silently undoing everything the framing just did. <code>handleResize()</code> is called too, for the trackball variants that cache the canvas&#39;s page box.</p><hr><h2 id="_3-pose-interpolation-—-controls-cameratransitions" tabindex="-1">3. Pose interpolation — <code>Controls/cameraTransitions</code> <a class="header-anchor" href="#_3-pose-interpolation-—-controls-cameratransitions" aria-label="Permalink to &quot;3. Pose interpolation — \`Controls/cameraTransitions\`&quot;">​</a></h2><p>Pure numeric helpers for moving a camera. <strong>Dependency-free</strong>: plain numbers and <code>[x, y, z]</code> tuples, no three.js objects in or out. That makes them unit-testable with no WebGL, and usable by a consumer that has its own copy of three without anything crossing that boundary.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">interface</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">Pose</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">position</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> [</span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">];</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">up</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> [</span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">];</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">/** The look-at point — the controls&#39; orbit pivot, not the camera&#39;s position. */</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">target</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> [</span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">];</span></span>
<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">interface</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">Pose</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">position</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> [</span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">];</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">up</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> [</span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">];</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">/** The look-at point — the controls&#39; orbit pivot, not the camera&#39;s position. */</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">target</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> [</span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">];</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><table><thead><tr><th>function</th><th>signature</th><th>purpose</th></tr></thead><tbody><tr><td><code>easeInOutCubic</code></td><td><code>(t: number) =&gt; number</code></td><td>Standard ease. Clamps out-of-range input.</td></tr><tr><td><code>viewPointToPose</code></td><td><code>(vp: CameraViewPreset) =&gt; Pose</code></td><td>Reads a view preset as a <code>Pose</code>.</td></tr><tr><td><code>interpolateFlightPose</code></td><td><code>(from: Pose, to: Pose, t: number) =&gt; Pose</code></td><td>Interpolates a camera between two poses. <code>t</code> is expected already eased.</td></tr><tr><td><code>orbitStepPose</code></td><td><code>(pose: Pose, yawRad: number, pitchRad: number) =&gt; Pose</code></td><td>Orbits around the pose&#39;s own pivot.</td></tr><tr><td><code>zoomPose</code></td><td><code>(pose: Pose, factor: number, minDistance?: number) =&gt; Pose</code></td><td>Scales the distance from the pivot.</td></tr><tr><td><code>poseDistance</code></td><td><code>(pose: Pose) =&gt; number</code></td><td>The orbit radius.</td></tr><tr><td><code>rotateAroundAxis</code></td><td><code>(v, axis, angleRad) =&gt; [number, number, number]</code></td><td>Rodrigues&#39; rotation.</td></tr><tr><td><code>orbitSwingAngle</code></td><td><code>(t: number, turns: number) =&gt; number</code></td><td>Swing angle for an entrance orbit.</td></tr></tbody></table><h3 id="why-interpolateflightpose-uses-a-quaternion-not-two-lerps" tabindex="-1">Why <code>interpolateFlightPose</code> uses a quaternion, not two lerps <a class="header-anchor" href="#why-interpolateflightpose-uses-a-quaternion-not-two-lerps" aria-label="Permalink to &quot;Why \`interpolateFlightPose\` uses a quaternion, not two lerps&quot;">​</a></h3><p>The pivot lerps from one target to the other, and the camera&#39;s <strong>orientation</strong> — both its direction from the pivot and its up vector — is carried by a single rotation built from the two poses&#39; full orthonormal bases. Not by interpolating <code>dir</code> and <code>up</code> independently.</p><p>That is a correctness requirement. Real preset data routinely contains <strong>antipodal up vectors on the same view axis</strong> — two views of one volume authored from opposite conventions, e.g. <code>up: [0,-1,0]</code> at eye <code>[0,0,2000]</code> and <code>up: [0,1,0]</code> at eye <code>[0,0,650]</code>. A plain <code>lerp(upFrom, upTo, t)</code> passes through the exact zero vector at <code>t = 0.5</code>, where <code>normalize</code> has no correct answer and falls back to an axis parallel to the view direction: <strong>the camera holds upside-down for the first half of the flight, then snaps 180 degrees in a single frame.</strong></p><p>Composing the two bases into a relative rotation and converting that matrix to a quaternion (largest-diagonal-term method) has no such degeneracy — for a valid rotation matrix at least one branch&#39;s divisor is bounded away from zero. The same case becomes a well-defined 180-degree roll about the shared axis: a smooth roll through the midpoint. The same construction also covers two antipodal camera <em>positions</em> about one target, where a vector-pair slerp flips back and forth near <code>t = 1</code>.</p><p><code>from.position</code> is reproduced exactly at <code>t = 0</code> and <code>to.position</code> at <code>t = 1</code>. <code>up</code> is likewise exact <strong>when</strong> each pose&#39;s own <code>up</code> is already perpendicular to its own view direction; one that leans toward the view axis is silently squared up, which is by design — it is not a meaningful camera roll.</p><h3 id="orbitsteppose-notes" tabindex="-1"><code>orbitStepPose</code> notes <a class="header-anchor" href="#orbitsteppose-notes" aria-label="Permalink to &quot;\`orbitStepPose\` notes&quot;">​</a></h3><p>The pivot and the orbit radius are both untouched, so a run of key presses can never walk the camera away from what it is looking at. Pitch is applied about the <strong>yawed</strong> right vector, not the original one — rotating about a stale axis makes a diagonal (left + up) key combination drift off the sphere instead of tracing it.</p><h3 id="orbitswingangle-notes" tabindex="-1"><code>orbitSwingAngle</code> notes <a class="header-anchor" href="#orbitswingangle-notes" aria-label="Permalink to &quot;\`orbitSwingAngle\` notes&quot;">​</a></h3><p><code>turns</code> is a <strong>swing amplitude, not a net rotation</strong>. <code>sin(t·π)</code> goes out and comes back to zero, so the camera always ends exactly on the framed preset view (<code>t = 1</code> → angle 0) however large <code>turns</code> is. Do not &quot;fix&quot; this into a net rotation; that ends the intro off-preset.</p><hr><h2 id="types" tabindex="-1">Types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;Types&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki github-dark vp-code-dark"><code><span class="line"><span style="color:#F97583;">interface</span><span style="color:#E1E4E8;"> </span><span style="color:#B392F0;">FitBounds</span><span style="color:#E1E4E8;"> {</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">width</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">;</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">height</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">;</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">depth</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">;</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#6A737D;">/** The box&#39;s centre in scene units. \`[0, 0, 0]\` for an NRRD volume. */</span></span>
<span class="line"><span style="color:#E1E4E8;">  </span><span style="color:#FFAB70;">center</span><span style="color:#F97583;">:</span><span style="color:#E1E4E8;"> [</span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">, </span><span style="color:#79B8FF;">number</span><span style="color:#E1E4E8;">];</span></span>
<span class="line"><span style="color:#E1E4E8;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">interface</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">FitBounds</span><span style="color:#24292E;"> {</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">width</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">;</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">height</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">;</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">depth</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">;</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#6A737D;">/** The box&#39;s centre in scene units. \`[0, 0, 0]\` for an NRRD volume. */</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#E36209;">center</span><span style="color:#D73A49;">:</span><span style="color:#24292E;"> [</span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">, </span><span style="color:#005CC5;">number</span><span style="color:#24292E;">];</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div>`,45),l=[t];function r(p,c,i,d,h,y){return e(),a("div",null,l)}const m=s(o,[["render",r]]);export{u as __pageData,m as default};
